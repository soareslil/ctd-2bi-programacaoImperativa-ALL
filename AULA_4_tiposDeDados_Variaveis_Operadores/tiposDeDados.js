let nome = "string com aspas duplas";

let sobrenome = 'strings usando aspas simples'; 

console.log(nome,'/', sobrenome);

//num inteiro
let idade = 22;

//num decimal usa ponto
let altura = 1.80;

//booleano true ou false nao pode usar aspas!
let diaChuvoso = false;
console.log(diaChuvoso);

let NaN = "not a number, não é um numero, é um valor que o js nao consgue processar";

let nulo = "null está dizendo que é o valro é nulo";

let undefined = "valor da variavel ainda nao foi definida";

let objetoLiteral = {
    propriedade: "valor",
    adicionandoNovaPropriedade: "conjuto de informacoes armazenaveis em uma unica variavel",
    lembre: "objeto literal sempre começa com chaves",
    doisPontos: "os dois pontos: funcionam como atribuição do valor a propriedade",
    retorno: "o retorno do objetoLiteral no console é por meio de chaves"
}

let array = ['tipoString', 10, 'array é uma lista', 'valores separados por virgulas', 'o indice começa no 0', 'aceita tipo string, numero', 'seu retorno no terminal é por meio de colchetes']